<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	   	   http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
           http://code.alibabatech.com/schema/dubbo
           http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <dubbo:application name="demo-provider" owner="leon" organization="yshop"/>
    <dubbo:registry address="127.0.0.1:2181" file="${user.home}/.dubbo/dubbo-registry-demo-provider.cache"/>
    <dubbo:provider timeout="12000"/>

    <!--uncomment this if you want to test dubbo's monitor-->
    <!--<dubbo:monitor protocol="registry"/>-->

    <dubbo:annotation package="com.bluemoon.kafka.dubbo"/>

    <!-- 以下port是netty使用的端口，同一台机部署多个dubbo服务时，netty要用不同端口-->
    <dubbo:protocol name="dubbo" serialization="dubbo" port="20882"/>

</beans>